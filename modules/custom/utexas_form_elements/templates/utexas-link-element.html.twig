{#
/**
 * @file
 * Custom theme implementation of a link with separate title and URL elements.
 *
 * Available variables:
 * - element.url: The link that has already been formatted by l().
 * - element.title: (optional) A descriptive or alternate title for the link, which may
 *   be different than the actual link text.
 *
 * @see template_preprocess()
 * @see template_preprocess_link_formatter_link_separate()
 *
 * @ingroup themeable
 */
#}
<div class="utexas-link-form-element">
  {{ element.url|spaceless }}
  {{ element.title|spaceless }}
</div>
