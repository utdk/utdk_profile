langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_utprof_listing_link
    - node.type.utprof_profile
id: node.utprof_profile.field_utprof_listing_link
field_name: field_utprof_listing_link
entity_type: node
bundle: utprof_profile
label: 'Link to profile page'
description: 'Set the behavior for this content when viewed in listings. Should that listing link to a full profile page?'
required: true
translatable: false
default_value:
  -
    value: 1
default_value_callback: ''
settings:
  on_label: 'Link title and image to profile page'
  off_label: 'Do not link title and image to anything'
field_type: boolean
