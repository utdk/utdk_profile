langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_utprof_basic_media
    - media.type.utexas_image
    - node.type.utprof_profile
id: node.utprof_profile.field_utprof_basic_media
field_name: field_utprof_basic_media
entity_type: node
bundle: utprof_profile
label: Image
description: 'For best results, upload an image of at least 500 pixels in width.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:media'
  handler_settings:
    target_bundles:
      utexas_image: utexas_image
    sort:
      field: _none
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
