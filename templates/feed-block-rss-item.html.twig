<div class="news-item">
  {% if date %}
    <span class="news-date">{{ date }}</span>
  {% endif %}
  <h4 class="news-headline">
    <a class="headline-link" href="{{ url}}">{{ title }}</a>
  </h4>
  {% if description %}
    <p class="news-description">{{ description }}</p>
  {% endif %}
</div>
